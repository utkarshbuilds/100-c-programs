#inlcude <stdio.h>

int main()
{
  //declaring the variables
  int a, b, sum=0;

  // taking input from user
  scanf("%d %d", &a, &b);

  // calculating the sum
  sum = a + b;

  // printing the sum 
  printf("%d", sum);

  return 0;
}
